#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;

int main() {
    int t,tc;
    scanf("%d",&tc);
    for (t=0; t<tc; t++)
    {
        int N=0;
        scanf("%d",&N);
        int* chessboard = (int*)malloc((sizeof(int) * N));
        for(int i=0; i<N; i++)
        {
            scanf("%d",&chessboard[i]);
        }
        for(int i=0; i<N; i++){
            if(chessboard[i]=N)
        }
        }
    }